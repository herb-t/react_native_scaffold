import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  layout: {
    position: 'absolute',
    justifyContent: 'center',
    alignItems: 'center',
    width: 350,
    height: 350,
    backgroundColor: '#fff',
    borderRadius: 6,
    elevation: 1
  },
  label: {
    fontSize: 24,
    color: '#000',
    textAlign: 'center',
    lineHeight: 48
  }
});
